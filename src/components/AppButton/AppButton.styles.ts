import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    height: 54,
    borderRadius: 8,
    marginBottom: 24,
    justifyContent: 'center',
  },
  buttonTitle: {
    fontSize: 16,
    fontWeight: '600',
    color: '#fff',
    textAlign: 'center',
  },
});
